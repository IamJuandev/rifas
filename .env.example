# Seed credentials for the first admin user (created on first DB boot)
ADMIN_USER=admin
ADMIN_PASSWORD=cambia-esto

# Secret used to sign the session cookie. Generate with: openssl rand -base64 32
SESSION_SECRET=

# Optional: override the SQLite file location
# DATABASE_PATH=./data/rifas.db
